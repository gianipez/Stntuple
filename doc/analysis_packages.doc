# -*- mode:org -*-

* How to create your own analysis package

#+begin_src
pkg=$USER                                      # strongly recommended
source Stntuple/scripts/make_stnana_dir $pkg   # if pkg is not specified on the command line, the created package name will be MyAna
source $pkg/scripts/build_config
scons lib/lib${pkg}_ana.so
#+end_src

